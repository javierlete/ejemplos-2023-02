import { CartelDirective } from './cartel.directive';

describe('CartelDirective', () => {
  it('should create an instance', () => {
    const directive = new CartelDirective();
    expect(directive).toBeTruthy();
  });
});
